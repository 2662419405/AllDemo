import React,{Component} from 'react'
import {
    Button
} from 'antd/dist/antd'

export default class className extends Component{
    render(){
        return(
            <div>
                <Button type="primary">
                    按钮
                </Button>
            </div>
        )
    }
}