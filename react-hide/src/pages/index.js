import React, { Component } from "react";
import Layout from "../layout/index";

export default class home extends Component {
  render() {
    return (
      <Layout title="用户" show={true}>
        用户
      </Layout>
    );
  }
}
